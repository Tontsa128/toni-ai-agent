# Projects

Each managed project should have an isolated workspace, explicit permissions and a small project memory record.

The agent should inspect a project's README, package manifest, configuration and tests before proposing changes.
