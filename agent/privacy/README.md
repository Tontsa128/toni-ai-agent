# Privacy controls

Raw screenshots are disabled. OCR content is transient and TTL-bound. Secret filtering is applied before model input for supported text content. Session/provider/cost budgets are enforced per InteractiveSession. Audit retention must preserve hash-chain integrity; do not delete a prefix of a single hash chain without a partition/anchor design.
