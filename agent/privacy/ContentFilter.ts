export interface FilterResult{blocked:boolean;value:string;reasons:string[]}
const P=[{name:"api-key",pattern:/\bsk-[A-Za-z0-9_-]{20,}\b/g},{name:"github-token",pattern:/\bgh[pousr]_[A-Za-z0-9_]{20,}\b/g},{name:"bearer-token",pattern:/\bBearer\s+[A-Za-z0-9._-]{20,}\b/gi},{name:"credit-card-like",pattern:/\b(?:\d[ -]*?){13,19}\b/g}];
export function filterSensitiveContent(value:string):FilterResult{const reasons:string[]=[];let filtered=value;for(const x of P){x.pattern.lastIndex=0;if(x.pattern.test(value)){reasons.push(x.name);x.pattern.lastIndex=0;filtered=filtered.replace(x.pattern,"[REDACTED]");}}return{blocked:reasons.length>0,value:filtered,reasons};}